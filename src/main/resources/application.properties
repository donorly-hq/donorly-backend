spring.application.name=donorly-backend
server.port=8080
spring.mongodb.uri=${SPRING_MONGODB_URI}
spring.mongodb.database=donorly

jwt.secret=${JWT_SECRET}
jwt.expiration-ms=86400000

# TEMPORARY — remove this line once MigrationController.java is deleted post-migration
migration.secret=${MIGRATION_SECRET}
